# 更新日志

## [0.4.49] - 2026-08-16

### 修复

- 对齐 Yunzai `Tools.js` 与最新版后端：文章列表显式提交 `page=1`、`limit=20`，合并所有分类后按发布时间降序展示；文章详情继续只提交数字 `threadID`，无效 ID 不再发出请求。
- 文章详情改用标准库 HTML 解析器提取段落、HTML 实体和正文图片，恢复封面、正文图片、摘要回退及标签展示；协议相对图片会补全为 HTTPS，重复图片、无效相对路径、脚本和样式内容会被过滤。
- 每日密码查询会忽略异常列表项并正确保留数字 `0`；公共账号池暂不可用时，定时推送不会提前标记当天完成，后续轮询可继续重试。

### 改进与验证

- 文章列表和详情在 OneBot 使用 `Node/Nodes` 合并转发，其他平台按正文顺序发送文本和图片；单篇正文最多展示 6000 字和 6 张图片，降低消息体积与图片带宽。
- 增加每日密码、权威分页参数、跨分类排序、20 篇上限、OneBot 节点、HTML 段落、封面、图片去重、无效 URL、图片上限、摘要回退和无效文章 ID 回归；同步更新 README、主帮助、功能矩阵和最近两版更新卡片。
- 本轮不新增图片素材，发布包继续保留 `resources/data` 并排除测试、开发文件、缓存和用户数据。

## [0.4.48] - 2026-08-16

### 修复

- 对齐 Yunzai `Price.js` 的多物品语义：`当前价格` 最多查询 5 个物品，`价格历史` 最多查询 3 个物品，`材料价格` 最多查询 5 个物品，`利润历史` 最多查询 10 个物品；支持名称、数字 ID、半角/全角逗号和 JSON 数组混合输入，并按顺序去重。
- `当前价格` 与 `价格历史` 改用最新版后端原生多 ID 参数一次查询；`材料价格` 与 `利润历史` 按接口约束受控逐项查询，单个物品失败不会丢失其他成功结果。
- 纠正旧版 Yunzai 的无效场所参数：最新版 `/api/v1/df/place/profit/history` 只接受 `objectID`，输入制造场所时会引导使用 `特勤处利润` 或 `利润排行`，不再发出后端不支持的请求。

### 改进与验证

- 多物品价格、材料和利润结果在 OneBot 使用 `Node/Nodes` 合并转发，其他平台返回完整分块文本；同时补充未找到、部分缺失、超出上限和最近价格明细提示。
- 增加混合解析、批量参数、逐项部分失败、场所纠正和 OneBot 节点序列化回归；同步更新 README、主帮助、功能矩阵和最近两版更新卡片。
- 本轮不新增图片素材，发布包继续保留 `resources/data` 并排除测试、开发文件、缓存和用户数据。

## [0.4.47] - 2026-08-16

### 修复

- `物品搜索 15010010001 15010010002` 等空格分隔多 ID 不再被误发为物品名称；同步支持半角/全角逗号和 JSON 数组形式，并按最新版 `/api/v1/df/object/search` 的 `objectID` 参数提交。
- 物品列表会区分空分类与页码越界，页码限制为 1-10000；后端返回总数但当前页为空时，会提示实际总页数而不是误报分类无数据。

### 改进与验证

- 物品列表和搜索补齐 Yunzai `Object.js` 已展示的价格、重量、稀有度和描述；OneBot 使用合并转发承载逐项详情，其他平台保留完整文本结果。
- 增加多 ID 参数、JSON 数组、名称空格、详细字段、分页越界和 OneBot 消息组件回归；同步更新 README、主帮助、功能矩阵和最近两版更新卡片。
- 本轮不新增图片素材，发布包继续保留 `resources/data` 并排除测试、开发文件、缓存和用户数据。

## [0.4.46] - 2026-08-16

### 修复

- `语音 渡鸦`、`语音 破壁`、`语音 标签=老赛` 等中文特殊语音标签不再被误判为角色；插件按需读取最新版 `/api/v1/df/audio/tags`，从描述及 `/、` 分隔别名建立映射并缓存 6 小时。
- 补齐 `event-*`、`festival-*`、`other-*`、`skin-*`、`enter-*`、`Hunt-*`、`commander-*`、`BF_*` 等最新版后端标签格式，直接输入原始标签时不会再错误请求角色接口。
- 标签元数据临时失败时仅缓存 60 秒并继续按角色查询，不阻断正常语音功能；显式 `角色=` 参数保持用户指定的角色语义，不会被中文标签自动改写。

### 改进与验证

- 语音消息补充角色职业、局内/局外场景、动作类型、动作细节和临时链接有效期，保持签名 URL 只存在于语音组件中，不在提示文本或日志中展开。
- 增加中文别名、标签缓存、完整标签格式、元数据失败降级、显式角色优先级和语音详情回归；同步更新 README、娱乐帮助、功能矩阵和最近两版更新卡片。
- 本轮不新增图片素材，继续保持 Playwright `1×` 渲染和低带宽输出；发布包继续排除测试、开发文件、缓存和用户数据。

## [0.4.45] - 2026-08-16

### 修复

- 对齐 Yunzai `Music.js` 的音乐记忆语义：最近歌曲、歌词、鼠鼠语音重播和点歌列表统一只保留 2 分钟，过期访问会清除对应内存并区分“尚未获取”和“已经过期”的提示。
- 鼠鼠音乐排行榜恢复每页 10 首及全局序号，第二页显示 `11-20`，可直接使用 `点歌 11` 等当前页序号播放；越界提示也按当前页实际序号范围生成。
- 对超过总页数的请求返回明确页码范围，不再笼统提示暂无歌曲；歌单名称、歌单 ID 和艺术家查询沿用最新版后端的合法参数。

### 验证

- 增加第二页全局序号选择、2 分钟边界、过期内存清理、歌词过期和重播随机回退测试，并将 `Music.js` 的 8 个处理器展开到本地功能矩阵。
- 同步更新 README、娱乐帮助和最近两版更新卡片；本轮不新增图片素材，继续保持 Playwright `1×` 渲染和低带宽输出。

## [0.4.44] - 2026-08-16

### 改进

- `tts状态` 在服务健康结果之外，并行展示角色预设数量、当前是否正在处理任务和等待队列长度，让状态查询与娱乐帮助中的功能说明保持一致。
- 队列或预设接口单独失败时降级为“暂不可用”提示，不影响已经取得的 TTS 服务健康状态；健康检查失败或缺少状态字段时仍沿用原有明确错误提示。

### 验证

- 增加队列与预设成功、附加接口失败降级、娱乐帮助文案和可选 Playwright 实际渲染回归；同步更新 README、娱乐帮助、功能矩阵和最近两版更新卡片。
- 本轮不新增图片素材，继续保持 Playwright `1×` 渲染和低带宽输出；发布包继续排除测试、开发文件、缓存和用户数据。

## [0.4.43] - 2026-08-16

### 新增

- 对接最新版后端主播巅峰赛模块，新增排行榜、主播搜索、竞猜候选、快照列表、快照详情、主播历史和主播统计 7 个独立只读命令，支持积分/击杀榜、第一/第二周、页码、平台、趋势、时间范围、统计周期和采样粒度参数。
- 排行榜与搜索展示主播平台、公开赛事 OpenID、仓库价值或击败数、破译数、对局数和排名趋势；快照、历史与统计查询限制单次展示数量，避免超长消息和额外带宽消耗。
- `巅峰赛竞猜` 仅查询题目、揭晓答案和候选主播，不接入会改变游戏状态的 `POST /api/v1/df/competition/dfs/predict` 竞猜提交接口。

### 改进与验证

- 搜索命令优先把首个位置参数作为昵称或 OpenID，避免昵称恰好为“抖音”“积分”等筛选词时被误判；显式参数仍支持 `关键词=`、`平台=`、`类型=` 等写法。
- 增加 7 个权威路由、参数边界、字符串榜单类型、成功、空数据、错误、只读候选池和命令前缀分发回归；同步更新 README、主帮助、功能矩阵和最近两版更新卡片。
- 本轮仅增加文本查询，不新增图片素材，继续保持 Playwright `1×` 渲染；发布包继续排除测试、开发文件、缓存和用户数据。

## [0.4.42] - 2026-08-16

### 修复

- 周报在图片渲染前通过最新版后端 `GET /api/v1/df/person/friendinfo` 补全好友与队友昵称、头像，同一 OpenID 只查询一次；单项失败、公共账号池暂无可用凭证或超过 30 人时保留脱敏 OpenID 尾号，不影响整张周报。
- 完整适配周报 `report1`、`report3`、`report4` 和 `bk` 扩展数据，补齐收益格式化、地图与干员名称、时长、最佳/最差队友、载具排行及支援统计；普通查询和定时推送共用相同数据处理。
- 修复好友带高价值物品时 `items` 字段与 Jinja2 字典方法冲突导致整张周报渲染失败的问题，并为长昵称增加安全换行。

### 改进与验证

- 最佳/最差队友与纯数据载具卡改为紧凑布局，减少大块空白；完整周报视觉夹具覆盖好友 TOP10、SOL/MP 队友、扩展统计和载具列表。
- 增加权威路由、字段映射、排序过滤、昵称解码、头像转换、去重、并发限制、异常隔离、查询上限和渲染前补全回归；同步更新 README、主帮助、功能矩阵和最近两版更新卡片。
- 本轮不新增图片素材，继续保持 Playwright `1×` 渲染；发布包继续排除测试、开发文件、缓存和用户数据。

## [0.4.41] - 2026-08-16

### 新增

- 新增 `集市兑换` 命令及 `集市`、`兑换列表` 别名，对接最新版后端 `GET /api/v1/df/fair`，展示奖励物品、所需兑换材料、绑定状态和起止时间，全程只读且不会执行游戏内兑换。
- 后端未返回商品标题时，按奖励和材料物品 ID 补全名称；查询失败时保留物品 ID 兜底，公共账号池暂无可用凭证时直接展示后端的明确提示。

### 改进与验证

- 单次最多展示 20 项集市记录，物品名称补全按出现顺序去重并限制为 40 个 ID，再合并为一次批量查询，避免超长消息和过多附加请求。
- 增加权威路由、命令注册与分发、字段映射、空数据、503、格式异常、名称补全失败和请求上限回归；同步更新 README、主帮助和最近两版更新卡片。
- 本轮仅增加文本查询，不新增图片素材，继续保持 1 倍渲染和低带宽输出；发布包继续排除测试、开发文件、缓存和用户数据。

## [0.4.40] - 2026-08-16

### 修复

- 恢复 Yunzai 原版特勤处信息语义：无参数时显示格式说明，指定设施和等级只渲染该等级，等级不存在时回退到当前可用最高等级。
- 指定设施但不指定等级时改为每个等级一张图片；`all` 查询按最新版后端支持的九种设施顺序分别输出，不再把多个等级塞入一张超长图片。
- OneBot 使用 AstrBot 原生合并转发组件按设施聚合，其他平台按标题和等级顺序逐条发送；关闭图片渲染或单张渲染失败时保留该等级的完整文字摘要，不影响其他等级。

### 改进与验证

- 特勤处信息模板输出宽度由 1700 像素收紧到 1280 像素，设施图、栅格、长名称、大数字、升级条件、材料、解锁物品、效果与详情均可安全换行，消除横向溢出和左右栏相交。
- 对照最新版后端确认支持仓库、指挥中心、工作台、技术中心、靶场、训练中心、制药台、防具台和潜水中心；不再展示后端已不支持的旧版收藏室。
- 增加命令语义、平台发送、单级失败隔离、正常模板和极端 DOM 布局回归；本轮不新增图片素材，继续使用 1 倍渲染并通过逐等级单图降低传输带宽。

## [0.4.39] - 2026-08-15

### 修复

- 修复战绩查询与战绩推送图片中超长地图名、干员名和模式名被固定单行布局裁切的问题，标题、状态和时间现在会在可用宽度内安全换行且互不遮挡。
- 战绩推送卡的数据层改为随内容增高，大额带出价值、净收益、得分、KDA、击杀明细和救援次数不再被固定高度截断。
- 帮助配置读取完成后会立即关闭文件，避免反复查看帮助时积累文件句柄或在 Windows 重载阶段产生资源警告。

### 改进与验证

- 队友信息改为弹性双区布局，超长干员名及大额价值、击杀、时长和救援数据可完整显示，不再挤成逐字竖排。
- 为 SOL、MP 推送卡和混合战绩列表增加 Playwright DOM 压力回归，检查水平/垂直溢出、截图边界和标题与状态相交；本轮不新增图片素材，继续使用 600 像素宽、1 倍渲染的低带宽输出。

## [0.4.38] - 2026-08-15

### 修复

- WebSocket 握手按最新版 Go 协议同时发送 `X-API-Key`、`X-Client-ID` 和 `X-Client-Type`，客户端频道身份不再只依赖订阅消息体补充。
- 战绩事件改为按推送目标独立去重，并仅在发送成功后记录；单个群或私信临时失败时，相同事件重发会只补送失败目标，不会重复打扰已成功目标。

### 改进与验证

- `ws状态` 现在区分未安装依赖、未配置 API Key、已停止、等待连接、正在订阅和已连接订阅；未配置 API Key 时会直接拒绝无效连接请求。
- 对照最新版 `plugins/ws/record` 与 `internal/ws/auth.go` 完成握手、订阅确认、异常清理、多目标隔离和失败补送回归；本轮不新增图片素材，保持 1 倍渲染和低带宽输出。

## [0.4.37] - 2026-08-15

### 修复

- 对齐最新版 QQ、微信和 Gamesafe OAuth 提交协议：完整地址使用 `callbackUrl`，当前会话内的单独授权码使用 `frameworkToken`、`authCode` 和 `state`，不再依赖旧版字段。
- OAuth 回调地址缺少 `state` 时可使用当前会话状态；单独授权码必须先获取授权链接，无会话、状态不匹配、链接已过期或格式无效时均会在请求前拒绝。
- 后端未返回过期时间时使用 10 分钟本地有效期，并自动清理过期或损坏的 OAuth 会话，避免异常会话阻断后续登录。

### 安全与验证

- OAuth API 错误统一隐藏完整回调地址、授权码、`state` 和 `frameworkToken`，继续对敏感消息执行自动撤回或手动撤回提醒。
- 完成 QQ、微信、Gamesafe 两种提交方式、会话校验、默认有效期、异常清理和错误脱敏回归；本轮不新增图片素材，保持 1 倍渲染和低带宽输出。

## [0.4.36] - 2026-08-15

### 新增

- 新增 `OCR价格` 与 `OCR价格历史`，接入通用 OCR 最新价和历史接口，支持名称/ID、统计天数、价格区间、涨跌及成色信息。
- 新增 `OCR材料价格` 与 `OCR材料历史`，接入制造材料 OCR 当前价和最长 3650 天历史查询。
- 新增 `OCR利润历史`、`OCR利润排行` 与 `OCR特勤处利润`，支持最新价、窗口最低价和制造成本三种材料计价模式。

### 修复与验证

- 修正利润查询未指定排序时错误默认使用时均利润的问题，现与后端统一默认按总利润排序；显式“时均”参数保持不变。
- OCR 查询默认要求插件 API Key，限制单次文本展示数量，并覆盖权威路由参数、成功、空数据、错误和越界分支。
- 同步更新 README、主帮助和最近两版更新卡片；本轮不新增图片素材，继续保持 1 倍渲染和低带宽输出。

## [0.4.35] - 2026-08-15

### 新增

- 新增 `弹药价格 [1-30天]`，接入 `GET /api/v1/df/object/ammo`，展示弹药当前价、区间均价、涨跌幅和有效价格覆盖情况。
- 新增 `制造材料列表 [成品名称/ID]`，接入 `GET /api/v1/df/place/material/list`，可查看全量材料或指定成品配方。
- 新增 `囤货建议 [阈值百分比]`，接入只读的 `GET /api/v1/df/place/material/stockpile`，展示兑换材料当前单位成本、窗口低位和原料构成，不执行兑换或写入库存。

### 改进与验证

- 查询天数和阈值均在请求前校验，列表限制展示数量，覆盖成功、空数据、API 错误和参数越界分支。
- 同步更新 README、主帮助和最近两版更新卡片；本轮仅增加文本查询，不新增图片素材或图片带宽负担。

## [0.4.34] - 2026-08-15

### 修复

- `ai评价` 先从最新版 `/api/v1/df/tools/ai/presets` 解析预设代码，支持代码、中文名及原版双向名称包含匹配，不再把后端不接受的中文名直接作为 `preset` 提交。
- 无效预设会列出最多 12 个可用名称和代码；预设列表查询失败时返回真实 API 错误，不再进入必然失败的评价请求。

### 改进与验证

- 增加同一用户、模式和预设组合的一小时冷却，以及请求中的 90 秒占位；不同模式或预设相互独立，API 失败或空正文会立即释放冷却。
- 新增 `ai_cooldown_seconds` 配置，支持管理员调整或关闭冷却；完成中文名、模糊匹配、无效预设、重复请求、失败释放和模式/预设隔离回归。
- 同步更新 README、主帮助和最近两版更新卡片；发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.33] - 2026-08-15

### 新增

- 为 `ai评价 <模式> <预设> [音色]` 增加独立的 `ai_tts_enabled`、访问模式及群/用户黑白名单配置，不再错误复用普通 TTS 的访问范围。
- AI 评价 TTS 的白名单和黑名单分别按用户或所在群任一命中处理，私聊不会匹配群列表。

### 修复与验证

- 对齐原版“AI 评价 TTS 使用评价全文”的语义，不再套用普通 `tts_max_length` 客户端限制；后端仍按权威 API 自身限制校验请求。
- 增加普通 TTS 关闭但 AI 白名单允许、AI 总开关拒绝、全文提交和独立权限提示回归；同步更新配置说明、帮助菜单和最近两版更新卡片。
- 发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.32] - 2026-08-15

### 改进

- TTS 角色列表补充角色描述、默认情感和可用情感数量，对齐原版角色预设列表的信息量。
- TTS 角色详情补充角色描述、默认情感、音色文件配置状态及每种情感的说明；仅展示配置状态，不输出后端音色文件路径。

### 验证

- 增加角色预设扩展字段、默认情感中文名和音色路径保护回归；同步更新娱乐帮助、README 和最近两版更新卡片。
- 发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.31] - 2026-08-15

### 新增

- 新增 `tts_enabled` 总开关，以及 `tts_access_mode`、`tts_group_list`、`tts_user_list` 群/用户黑白名单配置。
- 白名单模式下，用户或所在群任一命中即可合成；黑名单模式下，用户或所在群任一命中都会拒绝，私聊不会错误匹配群列表。

### 修复与验证

- 在提交 TTS 合成任务前执行访问检查，全局关闭、白名单未命中、用户黑名单和群黑名单分别返回明确提示；TTS 状态及角色预设查询不受限制。
- 访问列表统一兼容 WebUI 列表、数字 ID 及旧版逗号或换行文本配置；发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.30] - 2026-08-15

### 修复

- 修复 `语音 <角色> <场景> <动作>` 的位置参数解析；`语音 红狼 局内 战斗` 现在会分别提交 `character=红狼`、`scene=InGame`、`actionType=Combat`，不再把整段文本误作角色名。
- 对齐 Yunzai 的局内/局外、呼吸/战斗/死亡/受伤中英文映射，并支持角色语音、过场动画、环境音效、背景音乐等分类及 `boss-1`、`task-0` 等标签。
- 显式键值参数支持中英文键名并过滤未知字段；分类和标签走随机音频接口，角色、场景和动作组合走角色音频接口，统一只请求一条可播放语音。
- 语音角色列表兼容 `voiceId`、`voiceID`、`voiceid` 及对应干员 ID 字段，展示最多 12 个皮肤音色名称或 ID，避免静态映射字段形态变化时显示为 `-`。

### 验证

- 增加角色三段参数、中英文显式键值、分类、标签和小写角色映射字段回归；使用 AstrBot `Record` 组件发送音频的成功、空数据与错误分支继续通过。
- 发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.29] - 2026-08-15

### 改进

- 将账号删除、平台刷新、四类利润查询、投票、收藏、语音元数据、鼠鼠语音、WebSocket、战绩订阅、推送目标、广播、定时推送、房间和更新日志等不同业务语义，从其他命令的别名拆分为独立 AstrBot 处理器。
- 命令处理器由 133 个增加至 166 个，431 个命令名和别名仍保持唯一；WebUI 可以按真实业务入口分别展示和管理这些命令。
- `鼠鼠语音` 不再归属于歌词命令，`语音列表`、`标签列表`、`语音分类`、`语音统计` 也分别拥有标准命令入口。

### 修复与验证

- 对齐 Yunzai 管理权限语义，只有 AstrBot 管理员可以连接、断开或查看 WebSocket，以及管理广播通知入口；普通用户不会再改变全局连接状态。
- 恢复 `房间信息` 的原版开黑房间语义并明确标注最新版后端阻塞；已实现的只读战绩对局查询改为 `对局房间 [模式] [对局ID]`，避免同名命令含义被替换。
- 增加命令归属、权限拒绝、房间语义、唯一性和真实 AstrBot 注册回归；发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.28] - 2026-08-15

### 新增

- 新增 `任务物品`、`任务物品详情`、`重生任务线`、`重生任务线详情`、`重生前置组`、`重生前置组详情`、`重生文本配置` 七个独立 AstrBot 命令及对应别名。
- 任务物品支持 100 字以内关键词或物品 ID 搜索、后端分页与字符串物品 ID 详情查询；详情展示名称、类型、品质、材料、堆叠、尺寸、容量和可用属性。
- 重生资料支持任务线、前置组和文本配置分页，前置组可按任务线筛选；详情兼容字符串键任务映射、本地化文本、最终任务和空关联数据。

### 安全与验证

- 仅接入最新版 Go API 的 `GET /api/v1/df/quest/items`、`/item/:itemId` 和五个 `/rebirth/*` 只读接口；全部只使用 API Key，不要求玩家 Token，也不写入或改变游戏状态。
- 对关键词、字符串物品 ID、数字任务线/分组 ID、筛选值和页码执行边界校验；限制详情描述、任务列表、标签和 ID 列表长度，避免异常上游数据生成超长消息。
- 增加权威路由、路径编码、筛选、分页、成功、空数据、错误和命令唯一性专项测试；发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.27] - 2026-08-15

### 新增

- 新增 `收集任务组`、`收集任务奖励`、`收集任务槽位`、`随机收集任务` 四个独立 AstrBot 命令及对应别名，补齐最新版任务资料库的收集任务数据。
- 收集任务组支持 1–3 类型筛选并展示类型统计、物品及数量；奖励支持赛季筛选；槽位展示三类分组权重；随机任务展示后端选中的分组和已补全物品。
- 列表统一按每页 8 项展示，随机结果最多展示 12 个槽位、每组 8 个物品，并限制名称和描述长度。

### 修复与验证

- 修复带筛选任务命令的末尾裸页码解析，使 `赛季任务组 类型=1 2`、`命运契约 赛季=7 2` 等 README 示例按预期工作。
- 仅接入最新版 Go API 的四个 `GET /api/v1/df/quest/collector/*` 只读接口；全部只使用 API Key，不要求玩家 Token，也不写入或改变游戏状态。
- 增加权威路由、筛选、分页、成功、空数据、错误、随机单/多槽位、参数边界和命令唯一性测试；发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.26] - 2026-08-15

### 新增

- 新增 `任务目标`、`任务奖励`、`赛季阶段`、`赛季任务组`、`命运契约`、`任务统计` 六个独立 AstrBot 命令及对应别名，继续补齐任务资料库的组成数据与全库统计查询。
- 任务目标、奖励、赛季阶段、任务组和命运契约均按每页 8 项展示；任务组支持 `类型=数字` 筛选，命运契约支持 `赛季=数字` 筛选。
- 任务统计集中展示后端固定的 26 类资料计数；各列表兼容本地化字段、字符串化任务映射和空数据，并限制异常长文本及 ID 列表长度。

### 安全与验证

- 仅接入最新版 Go API 的六个 `GET` 只读接口：任务目标、任务奖励、赛季阶段、赛季任务组、命运契约和任务统计；全部只使用 API Key，不要求玩家 Token，也不改变游戏状态。
- 任务 ID、筛选值与页码均执行显式边界校验；完成权威路由、查询参数、成功、空数据、错误、分页越界、26 类统计和命令唯一性专项测试。
- 通过历史核心/登录回归及真实 AstrBot 注册验证；发布包继续排除测试、开发文件和缓存，不新增图片素材。

## [0.4.25] - 2026-08-15

### 新增

- 新增 `任务线详情`、`任务线树`、`赛季任务详情` 三个独立 AstrBot 命令及对应别名，补齐任务资料库的任务线内部查询。
- 任务线详情按每页 8 项展示任务名称、描述、等级和地图；任务线树按层级深度优先展开，每页 20 个节点并限制最多 200 个节点。
- 赛季任务详情按每页 3 个阶段展示阶段、主/子分组、任务、最终任务解锁条件和命运契约数量。

### 安全与验证

- 仅接入最新版 Go API 的 `GET /api/v1/df/quest/line/:lineId`、`/quest/line/:lineId/tree` 和 `/quest/season/line/:lineId`；不接入任务同步、跟踪或其他改变游戏状态的接口。
- 按权威后端字段处理本地化字符串、字符串化任务 Map、空数据、错误和分页越界，并限制异常长文本与树节点数量。
- 完成专项 fixture、历史核心/登录回归和真实 AstrBot 注册验证；发布包继续排除测试与开发文件，不新增高分辨率图片。

## [0.4.24] - 2026-08-15

### 新增

- 对接最新版任务资料库只读接口，新增 `任务线`、`任务搜索`、`任务详情`、`赛季任务` 四个独立 AstrBot 命令及对应别名。
- 任务线和赛季任务线支持分页；任务搜索支持关键词、任务类型、等级范围和分页；任务详情展示本地化名称、描述、目标、奖励及前后置任务。
- 任务资料库响应兼容后端本地化字符串对象和空数组，详情中的异常长文本会截断，避免单条消息过长。

### 安全与验证

- 仅接入最新版 Go API 的 `GET /api/v1/df/quest/lines`、`/quest/search`、`/quest/:questId` 和 `/quest/season/lines`，不接入任务同步、任务跟踪或任何改变游戏状态的接口。
- 按权威 API 契约固定查询参数、分页上限和响应字段，增加成功、空数据、错误、参数边界、本地化字段和命令唯一性测试。
- 发布包继续排除测试与开发文件，保持默认截图 `1×` 及已压缩素材策略不变。

## [0.4.23] - 2026-08-15

### 新增

- 对接最新版个人研究接口，新增 `研究中心` 独立 AstrBot 命令，以及 `个人研究`、`研究列表` 别名。
- 支持全部、未购买、已购买和大赚四种状态筛选及每页 8 项分页，展示目标物品、数量、价格、交换材料和购买状态。
- 仅对当前页物品 ID 去重补全名称；公共元数据不可用时保留原始 ID，确保研究主数据仍可读取。

### 安全与验证

- 只接入 `GET /api/v1/df/person/research` 只读查询；未开放会改变游戏状态的礼包兑换接口。
- 以最新版 Go API 的 `Item`、`ExchangedProps`、`Bought`、`Price`、`IsGreatEarn` 字段固定响应契约，并增加权威路由、筛选、分页、空数据、凭证失效和元数据降级测试。
- 清理发布包中的测试与开发专用文件，将大型地图、干员、特勤处和帮助素材转换为高质量 WebP，并把默认截图倍率由 `2×` 调整为 `1×`，降低插件包和消息图片带宽。
- 同步 README、帮助菜单、版本号和最近两版更新卡片。

## [0.4.22] - 2026-08-15

### 新增

- 对接最新版个人成就与成就分类接口，新增 `成就`、`成就分类` 两个独立 AstrBot 命令及荣誉、徽章等别名。
- 成就查询支持烽火/全面模式、分类名称或 `分类=ID` 筛选和每页 10 项分页，展示分类、成就 ID、最高徽章等级、描述与解锁条件。
- 分类元数据暂时不可用时保留个人成就查询，并以分类 ID 或“未分类”降级展示，避免次要接口故障阻断主数据。

### 安全与验证

- 以最新版 Go API 的 `honor`、`honorbox` 路由、`solutionType` 参数、`honorMap` 和分类 `list` 字段固定请求与响应契约。
- 限制单项描述与解锁条件长度，避免异常上游文本生成超长消息；增加模式、分类、分页、最高等级、空数据、凭证失效、分类降级和命令冲突测试。
- 同步 README、帮助菜单、功能矩阵、版本号和最近两版更新卡片。

## [0.4.21] - 2026-08-15

### 新增

- 对接最新版改枪社区的公开收藏夹、收藏夹详情、我的收藏夹、创建、更新、删除、添加/移除方案，以及被拒方案复审接口，新增 9 个独立 AstrBot 命令及对应别名。
- 收藏夹列表支持页码与作者筛选；详情显示公开状态、默认标记、方案数、获赞数和方案 ID；创建与更新使用竖线分隔可选描述和公开设置，避免空格描述被错误拆分。
- 复审结果、收藏夹可见性和错误提示统一使用中文，个人写操作继续使用 AstrBot 用户隔离标识。

### 安全与验证

- 按后端契约限制收藏夹名称 50 字、描述 500 字，并校验 24 位 ObjectID；默认收藏夹删除、每用户 20 个收藏夹、每夹 200 个方案及所有权继续由后端强制检查。
- 收藏夹详情、方案详情和浏览记录在统一认证下同步携带当前 AstrBot 用户隔离标识，确保作者可访问自己的私有内容且互动归属正确。
- 以最新版 Go 源码的收藏夹与复审 handler 固定路由、HTTP 方法、请求体、响应字段和权限边界。
- 增加权威路由、公开/个人查询、详情、CRUD、添加/移除、复审、参数边界、空数据、无权限和命令冲突测试；同步 README、帮助菜单、功能矩阵和最近两版更新卡片断言。

## [0.4.20] - 2026-08-15

### 新增

- 对接最新版改枪社区的“我的方案”、评论列表/发布/编辑/删除、浏览记录和复制记录接口，新增 6 个独立 AstrBot 命令及对应“改枪方案”别名。
- `改枪码详情` 成功后尽力记录浏览次数；`复制改枪码` 在写权限校验通过后返回可直接使用的改枪码，并记录复制次数。
- 我的方案支持模式、武器 ID、审核状态和页码筛选；列表与详情补充评论、浏览、复制和审核状态信息。

### 安全与验证

- 评论正文限制为 500 字，编辑和删除仅接受后端 ObjectID 格式；所有写操作继续携带 AstrBot 用户隔离标识，并由 `gunmod:community:write` 权限和后端所有权检查保护。
- 以最新版 Go 源码 `plugins/df/gunmod/community_plugin.go`、评论与社交 handler 固定路由、方法、请求体和响应字段。
- 增加权威路由、筛选透传、成功、空数据、无权限、无效 ID、浏览失败隔离和评论长度测试；同步 README、帮助菜单、功能矩阵和最近两版更新卡片断言。

## [0.4.19] - 2026-08-15

### 改进

- `藏品`、`资产` 命令迁移到最新版后端 `GET /api/v1/df/person/assets` 聚合接口，一次获取典藏枪皮、典藏挂饰和按 `secondClass` 动态分组的普通藏品。
- 直接使用后端补全并去重后的名称、图片、稀有度与分类，不再并发请求旧 v2 藏品和公共映射接口；保留枪皮、挂饰、干员皮肤等中文类型筛选。
- 资产卡片现在显示堆叠数量，文本兜底按实际数量汇总；空资产、筛选无结果、响应格式异常和服务错误均有独立提示。

### 验证

- 从 GitHub 刷新并确认 API `main` 最新提交仍为 `fbd0eb1`，以 `plugins/df/person/assets.go`、Swagger 和 `API.md` 固定聚合资产契约。
- 增加权威路由、典藏与普通资产合并、动态分类、类型筛选、URL 编码名称、稀有度、堆叠数量、空数据和错误响应测试。
- 同步更新 README、帮助菜单、功能矩阵和最近两版更新卡片断言。

## [0.4.18] - 2026-08-15

### 安全

- `ck登录 <cookie>`、`绑定 <frameworkToken>` 与显式 `角色绑定 <frameworkToken>` 现在会在调用后端前保护用户原消息：aiocqhttp 通过原生 `delete_msg` 尽力撤回，其他适配器、消息 ID 缺失或撤回失败时明确提醒立即手动撤回。
- 撤回结果不影响后续登录或绑定；Cookie 登录错误和远端绑定错误会再次过滤用户提交的完整凭证，日志只记录凭证类型与异常类型。
- 手动绑定缺少昵称和 UID 时统一显示“未命名账号”，不再把 frameworkToken 前缀当作账号名称回显。

### 验证

- 增加 Cookie 撤回成功、撤回失败、非 aiocqhttp 提示、手动 Token/角色绑定撤回及响应脱敏测试。
- 更新帮助菜单、README、功能矩阵与最近两版更新卡片断言。

## [0.4.17] - 2026-08-15

### 新增

- 对接最新版后端的腾讯游戏安全中心 Gamesafe OAuth，新增 `微信安全中心授权登录` 及三个英文/中文别名，复用敏感回调自动撤回和本地/远端账号绑定闭环。
- 新增微信安全中心绑定列表、登录信息、封禁记录、冻结状态、设备列表、在线状态和安全报告七类只读查询，共增加 8 个独立命令处理器。
- `删除 <序号>` 现在支持删除 Gamesafe 登录凭证并同步解除账号绑定。

### 安全

- Gamesafe 查询结果会过滤 Token、Cookie、OpenID、授权码、回调状态和其他凭证字段，不把后端原始敏感数据回显到聊天。
- 账号参数仅接受 5 至 20 位数字；本轮不暴露冻结、解冻和踢下线等会改变外部账号状态的接口。

### 验证

- 从 GitHub 拉取并确认 API `main` 最新提交仍为 `fbd0eb1`，以 `plugins/login/gamesafe`、`plugins/df/gamesafe`、Swagger 和 `API.md` 固定请求契约。
- 新增 Gamesafe OAuth、七类查询、专用凭证选择、空数据、凭证失效和敏感字段过滤回归测试。
- 命令注册扩展为 91 个独立处理器、332 个唯一命令名与别名，保持无重复、无空格命令和单消息单处理器命中。

## [0.4.16] - 2026-08-15

### 新增

- 对接最新版后端 v3.36.0 的 `GET /api/v1/df/activities`，新增独立命令 `活动日历` 及 `活动`、`活动列表` 别名。
- 新增活动日历图片模板，按后端标签与卡片顺序展示活动背景、类型、时间、奖励图片以及进行中、即将开始、已结束和时间待定状态。
- 图片渲染关闭或失败时返回完整文本摘要；公共账号池不可用、响应异常和空活动均提供明确提示。

### 验证

- 从已登录的 GitHub 仓库页面确认 API `main` 最新提交仍为 `fbd0eb1`，并以该提交的服务结构、Swagger 和 API 文档固定请求及响应契约。
- 新增权威路由、分组与扁平响应、相对图片地址、时间状态、空数据和服务错误回归测试。
- 命令注册扩展为 83 个独立处理器、311 个唯一命令名与别名，保持无重复、无空格命令和单消息单处理器命中。

## [0.4.15] - 2026-08-15

### 安全

- QQ/微信 OAuth 回调命令在 aiocqhttp 中会通过原生 `delete_msg` 尽力撤回用户提交的敏感消息，不记录消息内容、code 或 state。
- 非 aiocqhttp、消息 ID 缺失或协议端撤回失败时，授权与绑定流程继续执行，并明确提醒用户立即手动撤回。
- 即使回调 URL 不完整、会话过期、state 不匹配或后端拒绝授权，也会先尝试撤回已经提交的回调消息。

### 验证

- 增加自动撤回成功、协议端撤回失败、非 aiocqhttp 以及不完整回调的回归测试。
- 真实 AstrBot 隔离测试使用实际 `AiocqhttpMessageEvent` 验证消息 ID 会转换并传给 OneBot 撤回接口。

## [0.4.14] - 2026-08-15

### 修复

- 逐项反查 Yunzai `apps/**/*.js` 的全部命令正则，补齐基础与娱乐帮助的“功能”别名。
- 补齐 QQ、微信、WeGame、安全中心等扫码登录入口的“登陆”写法，以及 QQ/微信授权登录的全部前缀、授权方式和“登陆/登录”组合。
- 补齐 `插件强制更新`、`插件更新日志`，并让强制更新别名正确传递强制更新语义。

### 验证

- 新增 Yunzai 字面别名集合回归测试，并通过真实 AstrBot `CommandFilter` 唯一命中验证。
- 重新从 GitHub 同步三角洲 API 仓库，确认 `main` 最新提交仍为 `fbd0eb1d67731c99b012a999ee96cde47ad976c2`。

## [0.4.13] - 2026-08-15

### 新增

- 真实 AstrBot 隔离测试增加战绩主动推送路由，覆盖插件、`Context.send_message()`、UMO 会话解析和 aiocqhttp 适配器。
- 同一合成战绩事件同时投递群聊和私聊目标，验证文本与图片组件会被转换为合法 OneBot 消息段。

### 验证

- 群聊 UMO 正确路由到目标群号，私聊 UMO 正确路由到目标用户，图片文件在协议下发前转换为 `base64://` 数据。
- 重复战绩事件只下发一次；最终协议端使用内存 Bot 接收，不向真实群聊或好友发送测试消息。

## [0.4.12] - 2026-08-15

### 新增

- 真实 AstrBot 测试增加跨插件命令冲突验证，加载三角洲、终末地和内置命令的实际注册表。
- README 增加当前部署的命令管理映射，保留 Yunzai 命令语义，同时提供无冲突的三角洲专用命令名。

### 验证

- 确认本插件的 `help` 别名、`tts`、`干员列表` 在当前插件组合中分别会与其他处理器同时激活。
- 使用 AstrBot 原生 `rename_command()` 移除冲突别名，并将两个命令改为 `三角洲tts`、`三角洲干员列表`；改名后每条三角洲命令只激活一个 handler。

## [0.4.11] - 2026-08-14

### 新增

- 真实 AstrBot 测试增加完整唤醒阶段验证，使用多字符自定义触发符执行框架 `WakingCheckStage`。
- 验证框架去除触发符后仍向插件保留完整命令参数，并且只激活一个标准命令 handler。

### 验证

- `!!数据 烽火 2` 经真实唤醒阶段处理后得到 `数据 烽火 2`，插件读取内容与框架处理结果一致。
- `数据` handler 被唯一激活，参数记录只属于该 handler，没有前缀冲突或重复响应路径。

## [0.4.10] - 2026-08-14

### 新增

- 增加默认关闭的真实 AstrBot 隔离子进程测试，不使用框架替身导入插件并检查注册器。
- 注册测试逐一执行全部命令与别名的真实 `CommandFilter` 匹配，防止前缀冲突导致重复响应。

### 验证

- 插件使用本机 AstrBot v4.17.6 真实依赖成功导入，插件 ID、版本和注册类一致。
- 注册器包含 82 个消息 handler 和 82 个标准命令过滤器，不含 `RegexFilter` 或带空格命令名。
- 274 个命令与别名没有插件内重复，每条带参数消息均只命中一个 handler。

## [0.4.9] - 2026-08-14

### 新增

- 真实后端联调新增 19 组查询字段契约，覆盖公共元数据、物品、材料价格、音频、TTS、每日密码、文章、AI 预设和两套改枪方案接口。
- 每组契约仅断言响应类型与公开字段名，不保存或输出真实响应数据。

### 验证

- 将插件 82 个 HTTP 路径与 `delta-force-api-go` 最新 `main` 分支路由逐项对照，动态前缀与平台占位符均已人工复核。
- 19 组真实接口探测全部返回成功，字段结构与最新版 Go handler 和 Swagger 一致。
- 真实后端测试仍默认关闭，未注入 `DELTA_LIVE_API_KEY` 时不会联网。

## [0.4.8] - 2026-08-14

### 修复

- 修复 WebSocket 成功响应因 Go 后端省略零值 `code` 而被误判为订阅失败的问题。
- 修复 `kind=error` 的订阅拒绝响应被忽略并最终表现为等待超时的问题。

### 新增

- 增加默认关闭的真实后端联调测试，覆盖公共元数据、QQ 扫码初始化、QQ OAuth 初始化、已有绑定状态和 WebSocket 订阅握手。
- 联调测试增加网络日志静音与响应结构脱敏，只允许在断言中显示状态、消息类型和字段名。

### 验证

- 对照 `delta-force-api-go` 最新 `main` 分支源码与 `WS-API.md`，确认 `record.client.subscribe` 的请求和响应结构。
- 使用真实后端完成 5 项脱敏联调；WebSocket 成功升级连接并确认客户端级订阅频道。
- 真实扫码后的登录绑定闭环和实际 `record.new` 消息下发仍待交互验证。

## [0.4.7] - 2026-08-14

### 修复

- 修复插件重复初始化或旧实例未及时终止时可能并行运行多组 WebSocket 与定时推送任务的问题。
- 修复 WebSocket、HTTP 客户端或渲染器任一关闭失败时会阻断后续资源释放的问题。

### 优化

- 使用事件循环级任务登记表回收旧任务，并等待其退出后再启动当前实例任务。
- 后台任务增加实例化名称，便于在 AstrBot 日志和调试工具中定位来源。
- `terminate()` 增加任务与资源关闭超时、异常隔离和重复调用保护。

### 验证

- 增加旧实例任务替换、同实例重复初始化、任务登记、终止后禁止重启、关闭超时、关闭异常和重复终止测试。

## [0.4.6] - 2026-08-14

### 新增

- 增加全模板 Playwright 视觉验收模式，真实渲染 17 个业务模板和更新日志卡片。
- 视觉验收逐张检查 PNG 解码、尺寸范围、文件体积、可见像素和非白像素比例。

### 优化

- 全模板编译清单补齐个人信息与音乐列表，确保当前全部图片模板持续参与回归。
- 视觉测试完成后立即删除截图和临时 HTML，并验证浏览器与 Playwright 已正确关闭。
- README 增加快速回归和完整视觉验收命令，便于发布前复现检查。

### 验证

- 18 张模板截图均通过真实 Chromium 渲染和像素验收，未发现空白、透明、异常尺寸或资源加载失败。

## [0.4.5] - 2026-08-14

### 修复

- 显式启用 HTTPS 证书校验，并为请求超时提供独立、可读的错误提示。
- 正确识别无正文的 HTTP 204 成功响应，避免删除等操作被误判为失败。
- 非 JSON 服务端错误不再把完整 HTML 或大段响应正文返回给用户。

### 优化

- 文本资源改为流式下载，响应头或实际读取量超过上限时立即终止，避免先完整载入超大歌词等资源。
- 幂等查询可在不同备用地址之间故障切换，非幂等写请求仍保持单次提交。
- 网络异常日志仅记录请求方法、路径与异常类型，不记录凭证、请求正文或回调地址。

### 验证

- 增加 TLS 构造参数、文本编码、声明与实际大小上限、HTTP 错误、GET 故障切换、POST 超时、204、错误正文脱敏及客户端关闭测试。

## [0.4.4] - 2026-08-14

### 新增

- 货币流水按日期汇总资产余额，并使用 Playwright 渲染余额变化趋势图。
- 趋势图展示日期范围、起止余额、最高和最低余额、总变化及每日余额数据点。

### 优化

- 兼容斜杠日期、ISO 时间、逗号分隔金额、单日记录和无效余额，不让缺失数据污染趋势。
- 趋势图渲染失败时继续返回流水列表；关闭图片功能时保留原有文本流水摘要。

### 验证

- 增加多日聚合、时间排序、单日路径、无效余额、渲染失败隔离和模板编译测试。
- 使用 Playwright 实际渲染趋势图，并检查中文、折线、标签、汇总区、尺寸和非空像素。

## [0.4.3] - 2026-08-14

### 新增

- 战绩 WebSocket 订阅推送增加 Playwright 图片卡片，覆盖烽火地带与全面战场的核心战绩字段。
- 推送卡片展示玩家、地图、干员、时间、对局结果、收益、击杀、K/D/A、得分和救援等信息。

### 优化

- 同一战绩事件只渲染一次图片并复用于全部群聊或私信目标，避免重复启动渲染任务。
- 图片渲染失败或关闭图片功能时保留文本兜底，单个目标发送失败不影响其他目标。

### 验证

- 增加 SOL、MP、事件去重、图片关闭、渲染异常、多目标发送隔离和模板编译固定数据测试。
- 使用 Playwright 实际渲染战绩推送卡片并检查图片尺寸、内容完整性和非空像素。

## [0.4.2] - 2026-08-14

### 新增

- “更新日志”按 Yunzai 原版语义使用 Playwright 渲染最近两个版本的图片卡片。
- 增加结构化更新日志解析与版本一致性自动测试，避免发布时遗漏版本位置。

### 优化

- 更新日志图片使用本地背景与字体，渲染失败或关闭图片功能时保留 Markdown 文本兜底。
- 功能矩阵补充更新日志模板、解析和渲染验证证据。

## [0.4.1] - 2026-08-14

### 新增

- 新增 `房间信息 [模式] [对局ID]`，可查询烽火地带和全面战场的战绩对局成员详情。
- 接入 AstrBot 原生插件更新管理器和网页登录授权闭环。

### 修复

- 修复出红收藏模板素材路径和周报头像失败时的本地回退。
- 加固战绩 WebSocket 订阅确认、异常清理和推送隔离。
- 校准扫码、Cookie、OAuth 和网页登录的有效期、账号绑定及角色绑定结果提示。

### 验证

- 对房间详情的权威路由、烽火列表、全面嵌套玩家、空数据、错误和未绑定分支增加固定响应测试。
- 完整测试通过，真实 AstrBot v4.17.6 源码可导入并注册 82 个独立命令处理器。

## [0.4.0] - 2026-08-14

### 新增

- 完成原生 AstrBot 命令注册、帮助菜单和 Playwright 图片渲染。
- 接入 QQ、微信扫码与 OAuth 登录、多账号绑定、切换、刷新和解绑流程。
- 接入个人信息、战绩、日报、周报、藏品、流水、地图、特勤处和健康状态查询。
- 接入物品、价格、利润、改枪码、AI 评价、语音、音乐、TTS 和本地计算器。
- 实现日报、周报、每日密码、特勤处定时推送及战绩 WebSocket 推送。

### 修复

- 按最新版 Go 后端源码校正 API 路由、请求参数、响应字段和错误语义。
- 修复登录二维码消息类型、OAuth 回调、账号删除和主绑定同步流程。
- 扫码二维码按后端过期时间动态显示剩余秒数，并兼容秒、毫秒和 ISO 时间格式。
- 扫码、Cookie 与 OAuth 登录按账号绑定和角色绑定的真实结果提示，不再把本地暂存误报为完整绑定成功。
- 网页登录改用最新版后端数据授权协议，用户在授权页批准后由插件自动领取凭证并完成绑定。
- 修复 WebSocket 异常后的连接清理和定时推送单任务异常隔离。
- WebSocket 改用请求头传递 API Key，并校验客户端级战绩订阅确认响应。
- 补齐空数据、权限不足、凭证失效、接口错误和资源释放提示。

### 验证

- 建立 Yunzai 功能对照矩阵并覆盖全部 `apps` 源文件。
- 使用固定响应验证成功、空数据和错误分支，并对主要模板执行 Playwright 渲染检查。
